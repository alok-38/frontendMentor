document.addEventListener("DOMContentLoaded", () => {
  const formGroupErrors = document.querySelectorAll(".form-group__error");
  let errors;
  if (!errors) {
    formGroupErrors.forEach((error) => {
        
    })
  }
});
